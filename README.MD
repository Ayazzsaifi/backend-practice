# Backend Practice

## What I learned
- HTTP servers with Node.js
- Express routes (app.get, app.post)
- Query parameters (req.query)
- POST requests with req.body
- Middleware with app.use()
- CORS - connecting frontend to backend
- Full stack connection using fetch()
-JWT Tokens (AUTH)
-MongoDB
-project online course platform
-fixing more bugs 
-doing leatcode
